/**
 * @param {number[]} digits
 * @return {number[]}
 */
var plusOne = function(digits) {
 let finalNum = digits.join("");
    let num = BigInt(finalNum)+ BigInt(1);
    num = num.toString().split('').map((num) => parseInt(num));
    return num;
}
